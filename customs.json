{
  "functions": {
    "bern": {
      "args": 3,
      "operation": "((emin($1, 1.0) + elim($2, 0.0, $1) + elim($3, 0.0, 1.0)) * 0.0) + (fac($1) / (fac($2) * fac($1 - $2)) * pow($3, $2) * pow(1.0 - $3, $1 - $2))"
    }
  },
  "consts": {
    "SHIT": "2"
  }
}
