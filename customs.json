{
  "functions": {
    "bern": {
      "args": 3,
      "operation": "((fac(emin($1, 1.0)) / (fac(elim($2, 0.0, $1)) * fac(clamp($1 - $2, 0.0, $1)))) * pow($3, $2) * pow(elim(1.0 - $3, 0.0, 1.0), $1 - $2))"
    }
  },
  "consts": {
    "SHIT": "2"
  }
}
